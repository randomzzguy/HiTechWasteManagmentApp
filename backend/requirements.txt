# =============================================================
# Hi-Tech Waste Management — Backend Dependencies
# Python 3.11+
# =============================================================

# -------------------------------------------------------------
# Web Framework
# -------------------------------------------------------------
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
sse-starlette==2.1.0
websockets==12.0

# -------------------------------------------------------------
# Database — Async (FastAPI) + Sync (Celery)
# -------------------------------------------------------------
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9

# -------------------------------------------------------------
# Data Validation & Settings
# -------------------------------------------------------------
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# -------------------------------------------------------------
# Authentication & Security
# -------------------------------------------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1          # passlib is incompatible with bcrypt >= 4.1

# -------------------------------------------------------------
# HTTP Client
# -------------------------------------------------------------
httpx==0.27.0
aiohttp==3.9.5

# -------------------------------------------------------------
# Cache & Message Broker
# -------------------------------------------------------------
redis==5.0.4

# -------------------------------------------------------------
# Task Queue
# -------------------------------------------------------------
celery==5.4.0
celery[redis]==5.4.0

# -------------------------------------------------------------
# Vector Database
# -------------------------------------------------------------
pymilvus==2.4.0

# -------------------------------------------------------------
# MQTT Client
# -------------------------------------------------------------
aiomqtt==1.2.1

# -------------------------------------------------------------
# PDF Generation & Processing
# -------------------------------------------------------------
pdfplumber==0.11.0
weasyprint==62.3
reportlab==4.2.0
pillow==10.3.0
pytesseract>=0.3.13
pdf2image>=1.17.0
pytesseract>=0.3.13
pdf2image>=1.17.0

# -------------------------------------------------------------
# Templating
# -------------------------------------------------------------
jinja2==3.1.4

# -------------------------------------------------------------
# Data Processing
# -------------------------------------------------------------
pandas==2.2.2

# -------------------------------------------------------------
# Utilities
# -------------------------------------------------------------
python-dateutil==2.9.0
pytz==2024.1
uuid==1.30

# -------------------------------------------------------------
# Testing
# -------------------------------------------------------------
pytest==8.2.0
pytest-asyncio==0.23.7
anyio==4.4.0
aiosqlite==0.20.0
hypothesis==6.103.1
